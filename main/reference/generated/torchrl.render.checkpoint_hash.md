# checkpoint_hash

torchrl.render.checkpoint_hash(*path: str | Path*) → str[[source]](../../_modules/torchrl/render/checkpoint.html#checkpoint_hash)

Computes the SHA256 digest of a local checkpoint file.