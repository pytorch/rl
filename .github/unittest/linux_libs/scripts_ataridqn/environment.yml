channels:
  - pytorch
  - defaults
  - conda-forge
dependencies:
  - pip
  - gsutil
  - pip:
    - hypothesis
    - future
    - cloudpickle
    - pytest
    - pytest-cov
    - pytest-mock
    - pytest-instafail
    - pytest-rerunfailures
    - pytest-error-for-skips
    - pytest-asyncio
    - expecttest
    - pyyaml
    - scipy
    - hydra-core
    - tqdm
    - h5py
    - datasets
    - pillow
