channels:
  - pytorch
  - defaults
dependencies:
  - pip
  - pip:
    - hypothesis
    - future
    - cloudpickle
    - pytest
    - pytest-cov
    - pytest-mock
    - pytest-instafail
    - pytest-rerunfailures
    - pytest-timeout
    - pytest-xdist
    - expecttest
    - pybind11[global]
    - pyyaml
    - scipy
    - psutil
    - coverage
    - ray
