[tool.usort]
first_party_detection = false

[build-system]
requires = ["setuptools", "wheel", "torch"]

first_party_detection = false

target-version = ["py38"]

excludes = [
    "gallery",
    "tutorials",
]
