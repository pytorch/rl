channels:
  - pytorch
  - defaults
dependencies:
  - pip
  - cmake >= 3.18
  - mesa-libegl-cos6-x86_64
  - glfw
  - pip:
    - hypothesis
    - protobuf
    - future
    - cloudpickle
    - gym_retro
    - moviepy
    - tqdm
    - pytest
    - pytest-cov
    - pytest-mock
    - expecttest
    - pyyaml
    - scipy
    - dm_control
