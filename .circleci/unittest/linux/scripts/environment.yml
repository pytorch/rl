channels:
  - pytorch
  - defaults
dependencies:
  - pip
  - cmake
  - pip:
    - hypothesis
    - protobuf
    - future
    - cloudpickle
    - gym_retro
    - moviepy
    - tqdm
    - pytest
    - pytest-cov
    - pytest-mock
    - expecttest
    - pyyaml
    - scipy
    - dm_control
