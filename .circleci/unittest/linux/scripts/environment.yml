channels:
  - pytorch
  - defaults
dependencies:
  - python=3.8
  - pip
  - cmake >= 3.18
  - pip:
    - hypothesis
    - protobuf
    - future
    - cloudpickle
    - gym_retro
    - moviepy
    - tqdm
    - pytest
    - pytest-cov
    - pytest-mock
    - expecttest
    - pyyaml
    - scipy
    - dm_control
